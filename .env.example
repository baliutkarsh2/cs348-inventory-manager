# Copy to .env (if you use python-dotenv) or set in your hosting provider
SECRET_KEY=change-me
# For cloud DBs, use a URL like:
# DATABASE_URL=postgresql+psycopg://username:password@host:5432/dbname
# For local SQLite, default is sqlite:///app.db
